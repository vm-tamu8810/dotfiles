local colors = require("color").colors

return {
	colors = colors,
	use_fancy_tab_bar = false,
	show_new_tab_button_in_tab_bar = false,
	window_padding = { left = 0, right = 0, top = 15, bottom = 0 },
}

